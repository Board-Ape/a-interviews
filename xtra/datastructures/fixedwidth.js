/*

Fixed Width Integers
    - How many integers can you expres with 1 byte(8 bits) = 2^8 256 different numbers
    - 255 = 1111 1111 so 256 would be 1 1111 1111 
    Integer Overflow: 
        - Javascript converts the unit to infinity if it gets too large
    
    - Instead of using 1 byte we usually use 4 or 8 bytes (32 or 64 bits)

    - Most integers are fixed width or fixed length

*/