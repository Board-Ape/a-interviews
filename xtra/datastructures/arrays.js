/*

Arrays:
- RAM is essentially an array
- Arrays have their positives:
    - 0(1) lookups
- The negatives:
    - every item needs to be the same size
    - lots of uninterrupted free space in ram
    - adding and deleting is usually 0(n)

*/